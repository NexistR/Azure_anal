# Git ??????

?? `azure-churn-clv` ? Git ????????Notebook?Markdown/PDF ??????Power BI PBIP ?????????????????

???????????????????????????????????`.venv/`?`.runtime/`?`.tools/`?`.cache/`?`.pbi/`?`data/raw/` ? `data/processed/` ???????`models/` ????????? `.env`????? README ????`.env.example` ???????????

## ??

? PowerShell ??

```powershell
cd "G:\VScode\pj_mic\azure-churn-clv-git-upload"
git init
git add .
git status
# ?? status ??? .env?.venv?data/raw/*.csv ???????????
git commit -m "Add Azure churn CLV analysis project"
```

??? GitHub/GitLab ??????????????????????????

```powershell
git remote add origin <??????>
git branch -M main
git push -u origin main
```

W2 ??? HTML ???????Power BI ????? `dashboard/powerbi/W2_Churn.pbip`???????? Python ????? Power BI ??????? `data/raw/README.md` ???????????? `docs/operations/?????????.md` ???????
